# Instructions for running this app 

1. Open a terminal, and from the __parent__ directory `DrijvendeStenden`, run the `city-recognition/gateway.py` file
2. In __another__ git or powershell terminal, __first cd/navigate__ to `city-viewer` and then run `npx vite --port 8080 --host`